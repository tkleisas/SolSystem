namespace SolSystem.Client;

/// <summary>
/// Layout constants shared by the panels the client draws over its frame.
/// </summary>
internal static class FlightUi
{
    /// <summary>The line height every panel draws on.</summary>
    internal const float Line = 21f;
}
